# Production build config (picked up automatically by `vite build`).
# The deployed static app must know the backend's PUBLIC URL — same pattern as
# the EmberCheck apps (baked in at build time).
VITE_API_BASE=https://floodapi.samitweb.xyz
